/**
 * Configuración institucional de UNIDOLOR.
 * Fuente única: paquete @unidolor/core (monorepo packages/core).
 * Este archivo es un re-export de compatibilidad para los imports existentes
 * con el alias '@/config/institutionalConfig'.
 */
const { institutionalConfig } = require('@unidolor/core');

module.exports = institutionalConfig;
